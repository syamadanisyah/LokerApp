<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".splashScreen"
    android:background="@drawable/bglogin"
    android:layout_marginBottom="-150dp">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:id="@+id/logo"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/kalimat"
        android:text="aplikasi penyedia lowongan pekerjaan"
        app:layout_constraintTop_toBottomOf="@+id/logo"
        app:layout_constraintBottom_toTopOf="@+id/vectorhuman"
        android:textColor="@color/white"
        android:gravity="center"
        android:fontFamily="@font/poppinsbold"
        android:textStyle="bold"
        android:textSize="@dimen/_18sdp"/>

    <ImageView

        android:layout_width="@dimen/_325sdp"
        android:layout_height="@dimen/_280sdp"
        android:src="@drawable/tes"


        app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/kalimat"
        android:id="@+id/vectorhuman"/>

</androidx.constraintlayout.widget.ConstraintLayout>